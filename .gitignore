tmp/
*.tmp

# cockpit session decision logs (never commit — sensitive session transcripts)
.cockpit/

# release artifact generated by bin/release (uploaded to the GitHub release, never tracked)
SHA256SUMS
